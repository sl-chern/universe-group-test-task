{
  "name": "@testtask/database",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --skip-generate",
    "db:deploy": "prisma migrate deploy",
    "start:dev": "tsc --watch",
    "build": "tsc"
  },
  "devDependencies": {
    "@testtask/tsconfig": "*",
    "prisma": "^6.11.1"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1"
  }
}
